# Project Instructions

This readme is originated in a template repository for Django projects.

To init project properly:

## 1. Setup venv

## 2. Source to venv and run `init.sh`

```bash
./scripts/init.sh
```

This installs dependencies and applies migrations. For further launches you might use `start.sh`
